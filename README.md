# Kreoh PR project


# Useful git commands

- git status
- git add FILE1 FILE2
- git commit -m "Description of my new code changes"

How to create a new branch
- git branch
- git checkout -b NEW

# Other useful commands for the project
- git diff > diff.txt
- git stash

# Task 1
- There is a directory called `input` that contains a `diff.txt` file.
- The goal is to feed this `diff.txt` file to the LLM and generate a PR description.
- The PR description should be saved in a folder called `output` in a new file called `PR_DESCRIPTION.md`